servers:
  vars:
    ansible_user: anton
    ansible_become: true
  hosts:
    fedorable:
      ansible_host: 192.168.0.26 # Change when needed.
      ansible_ssh_private_key_file: "{{ ansible_env.HOME }}/.ssh/fedorable/"
    pisa:
      ansible_host: 192.168.0.27 # Change when needed.
      ansible_ssh_private_key_file: "{{ ansible_env.HOME }}/.ssh/pisa/"
